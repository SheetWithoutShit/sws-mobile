import { StyleSheet } from "react-native"


const styles = StyleSheet.create({
    reportHeader: {
        marginBottom: 30,
        marginTop: 10,
        display: "flex",
        flexDirection: "row",
    },

    button: {
        marginLeft: 10,
    },
})

export default styles
